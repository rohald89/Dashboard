
import styled from 'styled-components';

const NavStyles = styled.nav`
    grid-area: nav;
`

const Nav = () => {
    return (
        <NavStyles>
            Nav
        </NavStyles>
    )
}

export default Nav
