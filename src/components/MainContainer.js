
import styled from 'styled-components';

const MainStyles = styled.main`
    grid-area: main;
`

const MainContainer = () => {
    return (
        <MainStyles>
            Main
        </MainStyles>
    )
}

export default MainContainer
