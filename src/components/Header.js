
import styled from 'styled-components';

const HeaderStyles = styled.header`
    grid-area: header;
`

const Header = () => {
    return (
        <HeaderStyles>
            Header
        </HeaderStyles>
    )
}

export default Header
